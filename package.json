{
  "name": "luxon-moment-compat",
  "private": true,
  "scripts": {
    "test": "bun test ./test/moment.parity.examples.ts",
    "build": "bun build ./src/moment.ts --minify --target=browser --outfile=./dist/moment-wrapper.min.js"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/luxon": "^3.7.1",
    "@types/node": "^25.6.0",
    "moment": "^2.30.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "luxon": "^3.7.2"
  }
}
